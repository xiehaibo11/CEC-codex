可以，你这个系统应该加一个专门的数学核心，名字可以叫：

# **EVC-5M 数学决策算法**

全称：

```text
Event Contract 5-Minute Decision Algorithm
5分钟事件合约数学决策算法
```

它的目标只有一个：

```text
判断当前开多/开空，5分钟后到期价格高于还是低于当前价格的概率。
```

---

# 一、核心数学定义

假设：

```text
P_t = 当前开仓价格
P_{t+5} = 5分钟后的到期价格
```

定义未来5分钟收益：

```text
R_t^{5m} = ln(P_{t+5} / P_t)
```

做多胜负：

```text
Long Win = 1，如果 P_{t+5} > P_t
Long Loss = 1，如果 P_{t+5} <= P_t
```

做空胜负：

```text
Short Win = 1，如果 P_{t+5} < P_t
Short Loss = 1，如果 P_{t+5} >= P_t
```

所以你的数学模型核心不是预测涨跌多少，而是预测：

```text
Pr(P_{t+5} > P_t)
Pr(P_{t+5} < P_t)
```

也就是：

```text
Long_Probability = 未来5分钟上涨概率
Short_Probability = 未来5分钟下跌概率
```

---

# 二、输入因子向量

每一个时间点 t，系统生成一个因子向量：

```text
X_t = [
  trend_score,
  momentum_score,
  volume_score,
  volatility_score,
  orderbook_score,
  cvd_score,
  taker_ratio_score,
  open_interest_score,
  funding_score,
  support_resistance_score,
  breakout_score,
  pullback_score,
  range_score,
  fake_breakout_score,
  bull_trap_score,
  bear_trap_score,
  liquidity_sweep_score,
  wick_rejection_score,
  multi_timeframe_score,
  risk_score
]
```

每个因子统一归一化到：

```text
-100 到 100
```

含义：

```text
+100 = 强烈支持做多
0 = 中性
-100 = 强烈支持做空
```

---

# 三、因子归一化算法

所有原始指标不能直接相加，必须先归一化。

推荐公式：

```text
Z_i = (X_i - median(X_i)) / MAD(X_i)
```

然后压缩到 -100 到 100：

```text
Score_i = 100 * tanh(Z_i / 3)
```

解释：

```text
median = 中位数
MAD = Median Absolute Deviation，中位数绝对偏差
tanh = 双曲正切函数，防止极端值把模型拉爆
```

这样所有指标都能统一比较。

---

# 四、主要因子计算公式

## 1. 趋势分数

```text
TrendScore = 100 * tanh((EMA_fast - EMA_slow) / ATR)
```

例如：

```text
EMA_fast = EMA(9)
EMA_slow = EMA(21)
ATR = 平均真实波幅
```

含义：

```text
TrendScore > 30：短周期偏多
TrendScore < -30：短周期偏空
TrendScore 在 -20 到 20：趋势不明确
```

---

## 2. 动量分数

```text
MomentumScore = 100 * tanh(MACD_Histogram / Std(MACD_Histogram))
```

也可以加入 RSI：

```text
RSI_Score = RSI - 50
```

综合：

```text
MomentumScore = 0.6 * MACD_Score + 0.4 * RSI_Score
```

---

## 3. 成交量确认分数

```text
VolumeRatio = CurrentVolume / VolumeMA
```

```text
VolumeScore = 100 * tanh((VolumeRatio - 1) / 0.5)
```

突破时必须满足：

```text
VolumeRatio > 1.3
```

否则：

```text
突破信号降级为假突破风险
```

---

## 4. 盘口力量分数

盘口不平衡：

```text
OBI = (BidDepth - AskDepth) / (BidDepth + AskDepth)
```

转成分数：

```text
OrderbookScore = 100 * OBI
```

含义：

```text
OrderbookScore > 20：买盘更强
OrderbookScore < -20：卖盘更强
```

---

## 5. 主动成交分数

```text
TakerBuyRatio = TakerBuyVolume / TotalTakerVolume
```

```text
TakerScore = 100 * (TakerBuyRatio - 0.5) * 2
```

含义：

```text
TakerScore > 20：主动买入强
TakerScore < -20：主动卖出强
```

---

## 6. CVD 分数

```text
CVD_Delta = CVD_t - CVD_{t-n}
```

```text
CVDScore = 100 * tanh(CVD_Delta / Std(CVD_Delta))
```

判断背离：

```text
价格上涨，但 CVD 不上涨 = 诱多风险
价格下跌，但 CVD 不下跌 = 诱空风险
```

---

## 7. 震荡风险分数

先计算价格在区间中的位置：

```text
RangePosition = (CurrentPrice - RangeLow) / (RangeHigh - RangeLow)
```

如果：

```text
0.35 < RangePosition < 0.65
```

说明价格在震荡区间中间。

震荡风险：

```text
RangeRisk = 100 * (1 - TrendStrength) * (1 - 2 * abs(RangePosition - 0.5))
```

规则：

```text
RangeRisk > 60，禁止下单
```

---

# 五、假突破风险算法

## 向上假突破

```text
UpsideFakeoutRisk =
  0.25 * BreakAboveResistance
+ 0.20 * CloseBackBelowResistance
+ 0.20 * LowVolumeBreakout
+ 0.15 * CVD_Divergence
+ 0.10 * UpperWickRatio
+ 0.10 * OrderbookSellPressure
```

输出：

```text
FakeBreakoutRisk = 100 * UpsideFakeoutRisk
```

规则：

```text
FakeBreakoutRisk > 60：强制 HOLD
FakeBreakoutRisk > 40：禁止追多
```

---

## 向下假跌破

```text
DownsideFakeoutRisk =
  0.25 * BreakBelowSupport
+ 0.20 * CloseBackAboveSupport
+ 0.20 * LowVolumeBreakdown
+ 0.15 * CVD_Divergence
+ 0.10 * LowerWickRatio
+ 0.10 * OrderbookBuySupport
```

规则：

```text
DownsideFakeoutRisk > 60：强制 HOLD
DownsideFakeoutRisk > 40：禁止追空
```

---

# 六、多头陷阱算法

多头陷阱就是：看起来要涨，实际上是诱多。

```text
BullTrapRisk =
  0.20 * HighPositionRisk
+ 0.15 * LongUpperWick
+ 0.15 * FailedBreakout
+ 0.15 * CVD_BearishDivergence
+ 0.10 * TakerBuyExhaustion
+ 0.10 * OI_Rising_PriceNotRising
+ 0.10 * AskWallPressure
+ 0.05 * RSI_Overheated
```

规则：

```text
BullTrapRisk > 50：禁止做多
BullTrapRisk > 70：强制 HOLD
```

---

# 七、空头陷阱算法

空头陷阱就是：看起来要跌，实际上是诱空。

```text
BearTrapRisk =
  0.20 * LowPositionRisk
+ 0.15 * LongLowerWick
+ 0.15 * FailedBreakdown
+ 0.15 * CVD_BullishDivergence
+ 0.10 * TakerSellExhaustion
+ 0.10 * OI_Rising_PriceNotFalling
+ 0.10 * BidWallSupport
+ 0.05 * RSI_Oversold
```

规则：

```text
BearTrapRisk > 50：禁止做空
BearTrapRisk > 70：强制 HOLD
```

---

# 八、5分钟胜率概率模型

最终要算两个概率：

```text
P_long = 当前做多，5分钟后赢的概率
P_short = 当前做空，5分钟后赢的概率
```

推荐公式：

```text
P_long = sigmoid(
  β0
+ β1 * TrendScore
+ β2 * MomentumScore
+ β3 * VolumeScore
+ β4 * OrderbookScore
+ β5 * CVDScore
+ β6 * TakerScore
+ β7 * BreakoutScore
+ β8 * PullbackScore
- β9 * FakeBreakoutRisk
- β10 * BullTrapRisk
- β11 * RangeRisk
- β12 * ConflictRisk
)
```

```text
P_short = sigmoid(
  β0
- β1 * TrendScore
- β2 * MomentumScore
- β3 * VolumeScore
- β4 * OrderbookScore
- β5 * CVDScore
- β6 * TakerScore
+ β7 * BreakdownScore
+ β8 * PullbackScore
- β9 * FakeBreakoutRisk
- β10 * BearTrapRisk
- β11 * RangeRisk
- β12 * ConflictRisk
)
```

其中：

```text
sigmoid(x) = 1 / (1 + e^-x)
```

这一步可以先用规则权重，后面用历史回测训练 β 参数。

---

# 九、30 AI 数学投票模型

30 个 AI 不建议一开始全部用大模型 API。
最稳定的做法是：**30 个数学专家模型 + 一个 AI 解释层**。

每个 AI 输出：

```text
long / short / hold
```

单个 AI 的判断公式：

```text
AI_i_Direction =
  long，如果 P_i_long >= θ_i 且 Risk_i < RiskLimit_i
  short，如果 P_i_short >= θ_i 且 Risk_i < RiskLimit_i
  hold，否则
```

例如：

```text
Trend AI：
TrendScore > 35 且 MultiTimeframeScore > 30 = long
TrendScore < -35 且 MultiTimeframeScore < -30 = short
否则 hold
```

```text
Fake Breakout AI：
FakeBreakoutRisk > 50 = hold
否则根据突破方向判断
```

最终投票：

```text
LongVotes = count(AI_i = long)
ShortVotes = count(AI_i = short)
HoldVotes = count(AI_i = hold)
```

允许交易条件：

```text
LongVotes = 30，允许做多
ShortVotes = 30，允许做空
否则 HOLD
```

---

# 十、信号强度计算

```text
SignalStrength =
  0.25 * abs(TrendScore)
+ 0.20 * abs(MomentumScore)
+ 0.15 * abs(VolumeScore)
+ 0.15 * abs(CVDScore)
+ 0.10 * abs(OrderbookScore)
+ 0.10 * abs(MultiTimeframeScore)
+ 0.05 * HistoricalPatternWinRate
```

然后扣除风险：

```text
FinalSignalStrength =
  SignalStrength
- 0.30 * FakeBreakoutRisk
- 0.30 * TrapRisk
- 0.20 * RangeRisk
- 0.20 * ConflictRisk
```

规则：

```text
FinalSignalStrength >= 85，才允许下单
```

---

# 十一、事件合约期望收益 EV 算法

这个很重要。

假设：

```text
S = 单次下注本金
ρ = 胜利后的净收益比例
p = 当前方向胜率
f = 手续费比例
l = 滑点/延迟损耗比例
```

例如平台规则是：

```text
赢了赚 85%
输了亏 100%
```

那么：

```text
ρ = 0.85
```

期望收益：

```text
EV = p * ρ * S - (1 - p) * S - f * S - l * S
```

单位本金期望收益：

```text
EV_per_Stake = p * ρ - (1 - p) - f - l
```

整理：

```text
EV_per_Stake = p * (ρ + 1) - 1 - f - l
```

盈亏平衡胜率：

```text
P_break_even = (1 + f + l) / (ρ + 1)
```

例如：

```text
ρ = 0.85
f = 0
l = 0
```

那么：

```text
P_break_even = 1 / 1.85 = 54.05%
```

但你的系统不能只超过 54% 就下单，必须加安全边际。

建议：

```text
实际下单胜率门槛 = P_break_even + 8% 到 12%
```

也就是：

```text
最低胜率建议 >= 62% 到 66%
```

所以你的系统规则可以写成：

```text
P_win >= max(65%, P_break_even + 10%)
```

---

# 十二、最终下单数学公式

最终是否允许下单：

```text
AllowTrade =
  AIConsensusPassed
AND ProbabilityPassed
AND EVPassed
AND RiskPassed
AND MarketStatePassed
AND DataQualityPassed
```

展开：

```text
AllowTrade = true，当且仅当：

1. LongVotes = 30 或 ShortVotes = 30
2. P_win >= max(65%, P_break_even + 10%)
3. EV_per_Stake > 0
4. FinalSignalStrength >= 85
5. FakeBreakoutRisk < 30
6. TrapRisk < 30
7. RangeRisk < 40
8. ConflictRisk < 30
9. 数据完整
10. 当前价格不在震荡中间
```

---

# 十三、最终输出 JSON

你的系统每次计算输出：

```json id="0sl03x"
{
  "symbol": "BTCUSDT",
  "contract_type": "5m_event_contract",
  "entry_time": "2026-06-22 12:00:00",
  "entry_price": 65000.0,
  "expiry_time": "2026-06-22 12:05:00",
  "long_probability": 0.68,
  "short_probability": 0.32,
  "p_break_even": 0.5405,
  "expected_value_per_stake": 0.258,
  "final_direction": "long",
  "allow_trade": true,
  "long_votes": 30,
  "short_votes": 0,
  "hold_votes": 0,
  "signal_strength": 88,
  "fake_breakout_risk": 18,
  "bull_trap_risk": 22,
  "bear_trap_risk": 10,
  "range_risk": 25,
  "conflict_risk": 15,
  "reason": "30 AI全部支持做多，趋势、动量、成交量、CVD、盘口方向一致，假突破和多头陷阱风险低，5分钟做多期望收益为正。"
}
```

---

# 十四、回测训练算法

回测时每一个历史点都这样打标签：

```text
Label_long_t = 1，如果 P_{t+5} > P_t，否则 0
Label_short_t = 1，如果 P_{t+5} < P_t，否则 0
```

然后保存：

```text
X_t = 当前因子
Y_t = 5分钟后的胜负结果
```

训练目标：

```text
用 X_t 预测 Y_t
```

必须使用 walk-forward：

```text
过去30天训练
下一天测试
然后向前滚动
```

不能随机打乱时间序列。

---

# 十五、完整伪代码

```python id="2d7m6u"
for t in historical_times:

    # 1. 只能取当前时间之前的数据
    data_window = get_data_before(t)

    # 2. 计算因子
    factors = calculate_factors(data_window)

    # 3. 计算风险
    fake_breakout_risk = calc_fake_breakout_risk(factors)
    bull_trap_risk = calc_bull_trap_risk(factors)
    bear_trap_risk = calc_bear_trap_risk(factors)
    range_risk = calc_range_risk(factors)
    conflict_risk = calc_conflict_risk(factors)

    # 4. 计算未来5分钟胜率
    p_long = predict_long_probability(factors)
    p_short = predict_short_probability(factors)

    # 5. 计算30个AI投票
    ai_votes = run_30_ai_votes(factors)

    long_votes = count(ai_votes, "long")
    short_votes = count(ai_votes, "short")
    hold_votes = count(ai_votes, "hold")

    # 6. 计算事件合约期望收益
    p_win = max(p_long, p_short)
    ev = p_win * payout_rate - (1 - p_win) - fee_rate - slippage_rate

    # 7. 判断是否允许下单
    allow_long = (
        long_votes == 30 and
        p_long >= min_required_probability and
        ev > 0 and
        fake_breakout_risk < 30 and
        bull_trap_risk < 30 and
        range_risk < 40 and
        conflict_risk < 30
    )

    allow_short = (
        short_votes == 30 and
        p_short >= min_required_probability and
        ev > 0 and
        fake_breakout_risk < 30 and
        bear_trap_risk < 30 and
        range_risk < 40 and
        conflict_risk < 30
    )

    if not allow_long and not allow_short:
        continue

    direction = "long" if allow_long else "short"

    # 8. 事件合约结算
    entry_price = get_price_at(t)
    expiry_price = get_price_at(t + 5 minutes)

    if direction == "long":
        result = "win" if expiry_price > entry_price else "loss"

    if direction == "short":
        result = "win" if expiry_price < entry_price else "loss"

    save_backtest_trade(
        time=t,
        direction=direction,
        entry_price=entry_price,
        expiry_price=expiry_price,
        result=result,
        factors=factors,
        ai_votes=ai_votes,
        p_long=p_long,
        p_short=p_short,
        ev=ev
    )
```

---

# 十六、你系统最终采用的数学下单公式

可以直接写进项目文档：

```text
TradeSignal = 1 当且仅当：

Votes_direction = 30
P_win >= max(0.65, P_break_even + 0.10)
EV > 0
SignalStrength >= 85
FakeBreakoutRisk < 30
TrapRisk < 30
RangeRisk < 40
ConflictRisk < 30
DataQuality = true
```

否则：

```text
TradeSignal = HOLD
```

---

# 十七、最推荐的第一版实现

第一版不要复杂，先做这个：

```text
数据：Binance Futures 1m K线
周期：BTCUSDT
到期：5分钟
因子：趋势、动量、成交量、CVD、盘口、震荡、假突破、陷阱
模型：数学打分 + 30规则AI投票
回测：5分钟后胜负结算
输出：胜率、EV、连续亏损、假突破过滤效果
```

你的数学核心可以命名为：

```text
EVC-5M Score Engine
```

它最终只输出三个东西：

```text
LONG_5M_EVENT
SHORT_5M_EVENT
HOLD
```

这就是适合你这个系统的数学算法。
