最高优先风险

  1. hyperliquid_action_routes 疑似缺少认证/归属保护，却可返回交易 action payload：重点看
     backend/api/hyperliquid_action_routes.py:44。

  2. account_routes 有返回 LLM api_key 的风险点，需要确认是否明文暴露：看 backend/api/
     account_routes.py:225。

  3. Binance 客户端对非 testnet 环境默认走 mainnet，建议显式拒绝非法环境：看 backend/services/
     binance_trading_client.py:73。

  4. Program Trader 下单路径缺少 AI Trader 的 ±1% 价格 clamp：看 backend/services/
     program_execution_service.py:984。

  5. 启动逻辑过重：迁移、schema sync、数据修复、前端 watcher、后台服务都在 backend/main.py:462
     附近，生产变更要非常谨慎。

  6. Vite dev 配置和根脚本端口不一致：根后端 dev 是 5611，Vite proxy 指 8802，看
     package.json:10 和 frontend/vite.config.ts:22。

  规模与维护
  本地 scripts/code_split_audit.py 只读审计结果：超过 500 行的源文件 93 个，P0 14 个、P1 19
  个。最大文件包括 backend/services/hyper_ai_tools.py、backend/services/
  ai_decision_service.py、backend/services/hyperliquid_trading_client.py、frontend/app/
  components/signal/SignalManager.tsx。后续不要继续往这些大文件堆功能，优先小步拆分。
