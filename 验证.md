
• 本次我主要做了这些：

  1. Backtest Tool 回测改成后台任务
      - 点击“运行”后不再依赖页面里的单次请求。
      - 回测任务会写入数据库。
      - 页面刷新后会自动恢复任务并继续显示进度。
      - 只有点击“暂停”才会请求暂停。

  2. 新增回测任务 API
      - POST /api/event-contract/backtest/tasks 创建后台回测任务
      - GET /api/event-contract/backtest/tasks/{task_id} 查询任务进度和结果
      - POST /api/event-contract/backtest/tasks/{task_id}/pause 暂停任务
      - 原来的 /api/event-contract/backtest 同步接口保留，避免破坏旧调用。

  3. 新增回测任务数据库表
      - 新增 event_contract_backtest_tasks
      - 保存任务状态、进度、阶段、AI 状态、run_id、错误信息等。
      - 加入启动迁移流程。

  4. 前端 Backtest Tool 改造
      - 运行按钮变成任务启动。
      - 运行中显示暂停按钮。
      - 用 localStorage 保存当前任务 ID，刷新页面后继续恢复。
      - 每 2 秒轮询后端进度。
      - 回测完成后自动加载结果和交易明细。

  5. 新增 30 个 AI 工作状态显示
      - 回测结果区域新增 AI reviewer 状态面板。
      - 显示 30 个 AI 的 pending / running / completed / failed。
      - 防止“Backtest Tool 回测 AI 闲置看不出来”的问题。

  6. 数学算法按当前架构重写
      - 没有照文档硬写死。
      - 新增可配置进度算法。
      - 新增 Binance / CoinGlass 通用 API 限流算法。
      - 使用 token bucket，并读取响应头动态反馈。
      - 支持通过环境变量调整限制，不写死固定套餐。

  7. Binance / CoinGlass 请求限流
      - Binance adapter 接入统一限流器。
      - CoinGlass API 路由和特征采集接入统一限流器。
      - 支持 Retry-After、Binance weight header、CoinGlass remaining header。

  8. 修复本地 K 线保存重复键问题
      - 保存 K 线改为 upsert。
      - 避免本地运行时因为重复数据导致 SQLAlchemy session rollback。
      - 回测加载数据时会按需要做近期补齐。

  9. Signal System / CoinGlass 相关修复
      - 钱包跟踪方向从旧 Hyper Insight 逻辑切到 CoinGlass 方向。
      - CoinGlass 本地无数据、无 key、请求错误时的处理更明确。
      - CoinGlass 付费 key 仍然保持服务端处理，不暴露到前端。

  10. 测试和验证

  - 后端：uv run pytest -q，34 个测试通过。
  - 前端：pnpm build:frontend 构建通过。
  - 健康检查：http://127.0.0.1:5611/api/health 返回 200。
  - 前端本地服务：http://127.0.0.1:8802 可访问。
  - 实测创建回测任务、轮询进度、暂停接口都正常。