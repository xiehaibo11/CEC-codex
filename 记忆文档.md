# Hyper Alpha Arena 服务器记忆文档

更新时间：2026-06-21 UTC

## 当前部署状态

- 项目目录：`/root/Hyper-Alpha-Arena`
- 公网域名：`https://www.bocail.com`
- 应用运行方式：宿主机直接运行，已不使用 Docker
- 应用服务：`hyper-alpha-arena.service`
- 后端监听：`127.0.0.1:8802`
- PostgreSQL：宿主机 PostgreSQL 16，监听 `127.0.0.1:5432`
- Nginx：公网反代到 `127.0.0.1:8802`
- Docker：已卸载并清理 `/var/lib/docker`、`/var/lib/containerd`

## 关键文件

- systemd 服务：`/etc/systemd/system/hyper-alpha-arena.service`
- 环境变量：`/etc/hyper-alpha-arena.env`
- 加密密钥：`/var/lib/hyper-alpha-arena/.encryption_key`
- 前端构建产物：`/root/Hyper-Alpha-Arena/backend/static`
- Nginx 当前配置：`/etc/nginx/sites-enabled/bocail`
- 数据库备份：`/root/Hyper-Alpha-Arena/backups/alpha_arena_20260621T114236Z.dump`
- 快照库备份：`/root/Hyper-Alpha-Arena/backups/alpha_snapshots_20260621T114236Z.dump`

不要把 `/etc/hyper-alpha-arena.env` 和 `.encryption_key` 内容贴到聊天或日志里。

## 管理命令

```bash
systemctl status hyper-alpha-arena
systemctl restart hyper-alpha-arena
journalctl -u hyper-alpha-arena -f
systemctl status postgresql
nginx -t && systemctl reload nginx
```

健康检查：

```bash
curl http://127.0.0.1:8802/api/health
curl https://www.bocail.com/api/health
```

数据库检查：

```bash
PGPASSWORD=*** psql -h 127.0.0.1 -p 5432 -U alpha_user -d alpha_arena
```

## 已完成的重要修复

### Binance API Key 报错提示

用户在 AI Trader Management 添加 Binance key 时遇到：

```text
Invalid credentials: Binance API Error -2015: Invalid API-key, IP, or permissions for action
```

已修复为更明确的错误提示：

- 区分 Testnet/Mainnet key 场景
- 提示 Futures Demo Trading / USD-M Futures Mainnet key 必须匹配环境
- 提示如启用 IP 限制，需要白名单服务器出口 IP：`102.204.223.50`
- 后端保留 Binance 原始错误信息

涉及文件：

- `backend/services/binance_trading_client.py`
- `backend/api/binance_routes.py`
- `backend/config/settings.py`
- `frontend/app/components/trader/BinanceWalletSection.tsx`

### 登录授权 500

`报错.md` 中出现的错误不是 Binance key，而是登录授权链路：

```text
/api/get-app-login ... 500
/api/get-account ... 500
Unexpected end of JSON input
```

诊断结论：

- 500 来自 `account.akooi.com` 认证服务的间歇性异常
- 直接把错误空响应返回前端会导致 `response.json()` 崩溃

已修复：

- 前端新增 `authProxyProvider`，登录页走 `https://www.bocail.com`
- 后端新增认证代理重试：
  - `/api/get-app-login`
  - `/api/get-account`
- Nginx 只代理 OAuth 登录页、Casdoor 登录 JS/CSS 和登录相关 API
- 修复 `/callback` 中把 `TokenResponse` 当字符串使用的问题
- PKCE verifier 改为 64 位 URL-safe 字符串

涉及文件：

- `frontend/app/lib/auth.ts`
- `frontend/app/main.tsx`
- `frontend/public/auth-config.json`
- `backend/main.py`
- `/etc/nginx/sites-enabled/bocail`

验证结果：

```text
https://www.bocail.com/api/get-app-login -> 200, json=ok, app=hyper-alpha-arena
https://www.bocail.com/api/get-account -> 200, 未登录时返回 Please login first
```

## 非 Docker 部署过程摘要

已完成从 Docker 切换到宿主机直接部署：

1. 前端在宿主机执行 `pnpm --dir frontend build`
2. 将 `frontend/dist` 同步到 `backend/static`
3. 创建 Python venv：`backend/.venv`
4. 安装后端依赖：`backend/.venv/bin/pip install -e backend`
5. 从 Docker Postgres 导出数据库 dump
6. 恢复到宿主机 PostgreSQL 16
7. 创建并启用 `hyper-alpha-arena.service`
8. 停止 Docker app 和 Postgres
9. 验证公网后卸载 Docker

当前 Docker 已删除，不要再使用 `docker compose up` 部署本项目。

## 当前验证通过项

- `hyper-alpha-arena.service`：active/enabled
- `postgresql`：active
- `127.0.0.1:8802`：由 Python/uvicorn 监听
- `127.0.0.1:5432`：由宿主机 PostgreSQL 监听
- `https://www.bocail.com/api/health`：healthy
- 首页加载 bundle：`index-764aca32-1782075839226.js`
- `https://www.bocail.com/sub/jp-mm.yaml`：仍正常

## 回测工具当前实现

- 后端回测服务已从单文件拆分为 `backend/services/event_contract/` 模块，核心文件控制在 300-500 行内：
  - `config.py`：参数归一化与风控边界
  - `data.py`：K线读取、回测结果持久化、交易日志读取
  - `quality.py`：K线覆盖率、缺口、重复、未闭合K线、到期K线延迟审计
  - `rules.py`：规则预筛和过滤器
  - `analysis.py`：因子计算、市场状态、相似走势
  - `llm.py`：真实 AI 确认调用
  - `backtest.py`：事件合约回测主流程
- `event_contract_service.py` 只保留门面类和 mixin 组合。
- 前端回测页面已拆分为：
  - `BacktestTool.tsx`：状态、请求和页面组合
  - `BacktestConfigPanel.tsx`：回测配置
  - `PredictionPanel.tsx`：当前预测和数据质量
  - `BacktestResultsPanel.tsx`：回测结果、交易日志、AI/规则决策、过滤统计
  - `shared.tsx` / `types.ts`：格式化、指标卡、类型
- 回测结果包含 `engine_version=event-contract-v2`、`config_hash`、`data_quality`、`decision_bars_count`、`candidate_signals_count`、`missing_expiry_count`、`expiry_lag_skipped_count`。
- `rule_only` 使用本地 30 角色决策器 `system_30_ai`，每个可决策点都会生成 30 条文档指定 AI 名称的决策快照；真实外部 LLM 调用单独统计为 `llm_evaluated_count`。
- 共识规则按 `回测预测走势.md` 修正：只有 30/30 一致才允许 `LONG_5M_EVENT` / `SHORT_5M_EVENT`；28/29 只作为 watch 信号，不自动计入交易。
- 回测交易日志保存 `signal_type` 和完整 `event_signal` payload。
- 生产冒烟验证：本地 HTTP 回测返回 `run_id=19`，`total_trades=2`，`ai_evaluated_count=180`，`llm_evaluated_count=0`，第一笔 `LONG_5M_EVENT` 且投票 `30/0/0`。

## 注意事项

- 当前业务数据在宿主机 PostgreSQL，不在 Docker volume。
- Docker volume 已随 Docker 数据目录清理，保留的是 `backups/` 里的 dump 文件。
- 服务器环境禁止启动前端开发服务器，例如 `pnpm --dir frontend dev` 或 Vite dev server。修改前端后必须直接构建并同步到 `backend/static`，再重启 `hyper-alpha-arena.service`。
- 币种图标不要再逐个探测 Hyperliquid/CDN URL；已使用 `spothq/cryptocurrency-icons` 的 `svg/color` 和 Binance public asset logo crawl 复制到 `frontend/public/crypto-icons/`，通过 `/crypto-icons/manifest.json` 本地加载。前端 `CoinIcon` 无本地图标映射时显示字母徽标。
- Settings 交易监控图标审计：Binance Futures 可选币种 528 个，本地图标 790 个；当前能匹配 398 个，缺失 130 个，匹配率 75.38%。完整报告在 `docs/settings-icon-audit.json`，Binance 爬取报告在 `docs/binance-icon-crawl.json`。已修复大写 K 误删问题（如 KNC/KSM）、`IOTA -> MIOTA` 别名、数字前缀合约、`DODOX` 尾部 X 和 `LUNA2` 版本号映射。
- 后续同步 Binance 图标可执行 `pnpm sync:icons`，脚本为 `scripts/sync-binance-icons.mjs`；执行后仍需重新 `pnpm --dir frontend build` 并同步 `frontend/dist` 到 `backend/static`。
- Binance BTC 资金流已用 1m futures Kline taker buy/sell 数据回填到 `market_trades_aggregated`：`2025-06-01T00:00:00Z` 到 `2026-06-21T19:45:00Z`，共 555586 条。资金流覆盖接口 365 天窗口 zero=0。后续可用 `scripts/backfill_binance_flow.py --symbol BTC --start 2025-06-01 --end now` 单币种补采。
- Binance K线覆盖接口已修复 `3d/1w/1M` 长周期显示：长周期 K 线按整根 K 线覆盖的日期范围标记，不再只把开盘日显示为 100%。BTC 的 `1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M` 365 天覆盖窗口均 zero=0。覆盖接口现在返回 `summary`，包含窗口、覆盖天数、缺失天数、平均覆盖率、覆盖口径；Settings 前端已显示这些摘要，并将“资金流覆盖”标注为 `1m 主动买卖`，避免和 `1M` 月线混淆。
- CoinGlass 页面已修复数据集切换状态：点击左侧核心数据项会立即使用该数据集默认参数重新请求对应 CoinGlass API，并在加载时清空旧图表；连续快速切换时旧请求返回不会覆盖当前选择，避免不同接口数据显示停留在上一个结果。
- 回测工具已接入 CoinGlass 历史特征层：`backend/services/event_contract/coinglass_features.py` 统一分页拉取 Aggregated CVD、Pair taker buy/sell、Aggregated OI、Funding、Pair liquidation；回测开始时一次性加载并按时间戳对齐，不在每根 K 线循环里请求外部 API。
- 回测 CoinGlass 配置项：`enable_coinglass_features`、`min_coinglass_coverage_pct`（默认 96）、`strict_coinglass_quality`、`coinglass_no_future_leakage`。严格模式下任一 CoinGlass 指标覆盖率低于阈值会 400 阻断，不会用空数据生成结果。
- 回测因子和 30 AI 规则已改为真实 CoinGlass 优先：CVD/taker/OI/Funding/清算有 CoinGlass 数据时使用真实指标；无数据时才标记 `proxy_data` 并回退 OHLCV 代理。
- 前端 Backtest Tool 已增加 CoinGlass 历史因子开关、严格覆盖率、96% 阈值输入，并在预测/回测结果里显示 CoinGlass 覆盖率、key 来源和各指标覆盖率。启用 CoinGlass 时会自动打开 CVD 确认过滤。
- AI 工具 `predict_event_contract_5m` 和 `run_event_contract_backtest` 已增加 CoinGlass 参数，AI 交易员可显式调用。前端登录用户在 CoinGlass 页面保存的个人 key 会被回测 API 临时使用；AI 工具调用走服务器 `COINGLASS_API_KEY`。
- 2026-06-21 生产验证：未启用 CoinGlass 的 `/api/event-contract/predict` 正常返回；启用 CoinGlass + `1m` + 严格 96% 时当前 key/API 计划返回 interval 不可用，系统按设计返回 400 并列出 5 个 CoinGlass 指标覆盖率 0%，没有静默降级为假数据。
- Backtest Tool 配置已增加“保存配置”按钮，保存到浏览器 localStorage：`hyper-alpha-arena:backtest-tool:config:v1`。刷新页面后会自动恢复上次保存的币种、周期、资金、过滤器、CoinGlass 开关等配置；不会保存任何 CoinGlass API key。
- 事件合约回测时间口径已修正：数据库 K 线 `timestamp` 是开盘时间，但回测使用该 K 线 `close` 作为入场/到期价格，因此展示、AI prompt、event_signal、trade log、equity_curve 统一改为“收盘后决策时间 = timestamp + period”。延迟入场也按收盘后决策时间计算。
- 事件合约回测新增精度校验：主 K 线周期不能大于事件到期窗口。例如 `period=30m` + `expiry=5m` 会返回 400，避免把 30m K 线误当 5m 事件结算。前端会按 expiry 自动过滤不可用周期。
- CoinGlass 无未来泄露对齐已修正：当前 K 线收盘后只允许使用同一根已完成 CoinGlass 区间或更早数据，不再错误地往前错一根 K 线；`coinglass_no_future_leakage=false` 才允许调试性使用下一根区间。
- 回测工具已接入本地 L2 盘口特征层：新增 `backend/services/event_contract/l2_features.py`，从 `market_orderbook_snapshots` 读取本地 Binance/交易所 orderbook 快照，按“收盘后决策时间”取不晚于当前时间的最近快照，输出 L2 depth ratio、orderbook imbalance、spread bps；默认 no-future-leakage。
- L2 配置项：`enable_l2_features`、`min_l2_coverage_pct`（默认 96）、`strict_l2_quality`、`max_l2_lag_seconds`。前端 Backtest Tool 默认开启本地 L2 盘口因子和 strict 96%，预测/回测结果展示 L2 覆盖率、最大延迟和快照数；AI 工具 `predict_event_contract_5m` / `run_event_contract_backtest` 默认带 L2 参数。
- 30 AI 规则已改为真实 L2 优先：Orderbook AI 使用 `orderbook_imbalance`，Spread AI 使用真实 bid/ask spread；只有未启用或缺失 L2 时才退回 OHLCV proxy 并标记 `proxy_data`。Factor Snapshot 增加 `L2 Orderbook Imbalance`、`L2 Depth Ratio`、`L2 Spread bps`。
- Binance orderbook 采集保存已修正：`UnifiedOrderbook` 和 `ExchangeDataPersistence.save_orderbook` 现在分别保存 top5/top10 深度、订单档数和 raw top10 levels；老数据里 top5/top10 可能相同，新采集数据会区分。
- 2026-06-21 生产验证：Binance/BTC 本地 L2 当前只有当日约 09:34 UTC 后的快照，最近预测 80 根 1m 决策 K 线 L2 覆盖 100%，最大延迟约 26 秒；45 分钟 HTTP 回测 strict L2 覆盖 100%。选择 24 小时 strict L2 回测会按设计失败，示例覆盖约 47.99% < 96%，不会用缺失 L2 生成结果。
- `报错.md` 中的 `/api/event-contract/predict 400` 已定位：前端 `apiRequest` 会吞掉后端 `detail`，只显示 `HTTP error! status: 400`；已修正为保留后端错误详情。另一个问题是预测接口曾用 80 根 warmup K 线校验 L2 覆盖，导致 `15m/30m` 预测被本地 L2 历史不足误挡；已改为预测只校验最新决策点 L2，回测仍按完整回测区间 strict 校验。
- CoinGlass 回测因子增加会员能力检测：新增 `/api/coinglass/event-contract-capability`，按当前用户个人 key 优先、否则服务器 key，实测 Aggregated CVD、Pair taker volume、OI、Funding、Liquidation 五个核心指标是否支持当前周期。Backtest Tool 会自动检测，未开放时自动关闭并禁用 CoinGlass 开关；升级到支持该周期的 CoinGlass 会员/key 后会自动开放。2026-06-21 当前服务器 key 为 `STARTUP`，`1m/3m/5m/15m` 不支持，需要 `Standard`，`30m+` 可用；事件合约短周期默认不再触发 CoinGlass 400。
- 修改前端后，需要重新执行：

```bash
pnpm --dir frontend build
rm -rf backend/static
mkdir -p backend/static
cp -a frontend/dist/. backend/static/
systemctl restart hyper-alpha-arena
```

- 修改后端 Python 后，一般只需：

```bash
systemctl restart hyper-alpha-arena
```

- 修改 Python 依赖后：

```bash
backend/.venv/bin/pip install -e backend
systemctl restart hyper-alpha-arena
```
